# 阅读页左侧章节目录实施计划

## 概要

为 PDF 和 EPUB 阅读页增加默认展开、可收起的左侧章节目录。点击目录项后跳转到对应实际位置，并高亮当前章节。PDF 优先使用内嵌大纲；缺失时自动识别目录页和正文标题。

## 关键改动

- 新增 `GET /api/books/{book_id}/toc`，返回 `items`、目录来源和索引完成状态，仅允许书籍所有者访问。
- PDF 优先解析原始文件的多级书签；无书签时从已有原生文字或 OCR 页面索引识别目录，并通过正文标题匹配校正印刷页码到实际 PDF 页索引的偏移。
- 阅读器适配器增加统一的目录数据、导航目标和当前章节回调；PDF 复用现有页码跳转，EPUB 使用 `rendition.display(href)`。
- PDF/EPUB 桌面端默认显示左侧目录，可收起并与右侧搜索、批注、设置栏共存；移动端使用选择后自动关闭的抽屉。
- 提供加载中、识别中、识别失败和未识别到目录状态；目录不可用时保留现有页码或翻页导航。

## 自动识别规则

- 对文本执行 Unicode NFKC、数字间空格和多余空白归一化。
- 在文档前部候选页中寻找“目录/Contents”和至少三个带尾部页码的条目，合并跨行标题。
- 优先用正文页面开头的标题精确匹配目录项，按匹配结果计算印刷页码偏移；未直接匹配项使用经过验证的公共偏移。
- 找不到目录页时，仅从各页开头识别“第 X 章”“X.Y”等高置信度标题。
- 过滤重复、越界、倒序或低置信度结果；OCR 未完成时返回不完整状态并在完成后刷新。

## 测试计划

- 后端覆盖内嵌多级书签、无书签目录识别、页码偏移、跨行标题、过滤规则、OCR 状态及权限隔离。
- 前端覆盖目录加载与层级、当前章节高亮、PDF 单页/连续模式跳转、EPUB href 跳转、左右侧栏并存和移动端抽屉。
- 使用仓库根目录两本 202 页且无内嵌书签的教材验证自动识别和实际跳转位置。
- 运行后端测试、前端 Vitest、TypeScript 构建和 ESLint。

## 范围与默认值

- 本期仅覆盖 PDF 和 EPUB；TXT、Markdown 不生成章节目录。
- 识别完全在本地完成，不调用外部 AI 服务，不增加手工目录编辑功能。
- 不新增数据库表；目录从原书、书签及现有文本/OCR 索引生成，由前端查询缓存。
